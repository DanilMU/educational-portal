✅ Что МОЖЕТ делать MODERATOR:
1. Управление учебным контентом
Subjects: Создание, обновление, удаление тем.
Topics: Создание, обновление, удаление подтем.
Lessons: Создание, обновление, удаление уроков.
Quizzes: Создание, обновление, удаление тесты.
Это его основная функция — наполнять платформу контентом. 

2. Просмотр прогресса студентов
Progress: Просмотр прогресса (GET /progress/{id}) и списка прогресса (GET /progress) — но только своего или в рамках своих курсов, если система позволяет.
⚠️ Важно: Не должен видеть все прогрессы всех пользователей, если нет явной бизнес-необходимости. Лучше ограничить доступ только к прогрессу студентов, проходящих его курсы. 

❌ Что НЕ должен делать MODERATOR:
1. Управление пользователями
Users: НЕ должен видеть список всех пользователей (GET /users), создавать (POST /users), обновлять (PATCH /users) или удалять (DEL /users) пользователей.
Это привилегия ADMIN. 

2. Управление аутентификацией
Auth: Может использовать эндпоинты Login, Refresh, Logout для управления своей сессией. Как и любой пользователь, он должен иметь возможность входить и выходить из системы. Запрет распространяется на управление другими пользователями, а не на использование базовых функций.

3. Полный просмотр прогресса
GET /progress — если этот эндпоинт возвращает весь список прогресса всех пользователей, то модератору он не нужен. Если же он возвращает только прогресс по его контенту — тогда можно разрешить.




🧑‍🎓 STUDENT — что может и что не может
Студент — это конечный пользователь, который проходит обучение. Его права должны быть максимально ограниченными.

✅ Что МОЖЕТ делать STUDENT:
1. Авторизация
POST /auth/login — вход в систему.
POST /auth/refresh — обновление токена.
POST /auth/logout — выход из системы.
2. Получение информации о себе
GET /users/me — получить свои данные.
3. Просмотр учебного контента
GET /subjects — список всех предметов.
GET /topics — список всех тем.
GET /lessons — список всех уроков.
GET /quizzes — список всех тесты.
GET /subjects/{id}, GET /topics/{id}, GET /lessons/{id}, GET /quizzes/{id} — детали.
4. Работа со своим прогрессом
GET /progress — посмотреть список своего прогресса.
GET /progress/{lessonId} — посмотреть свой прогресс по конкретному уроку.
Студент может только просматривать свой прогресс. Любые изменения (создание, обновление, удаление) инициируются сервером автоматически, например, после сдачи квиза.

❌ Что НЕ должен делать STUDENT:
1. Управление контентом
Никаких POST, PATCH, DELETE для subjects, topics, lessons, quizzes и progress.
2. Управление пользователями
Никаких действий с /users.
3. Просмотр чужого прогресса
Только GET /progress/{id} — если {id} относится к его собственному прогрессу. //Todo: подумать над прогрессом



❌ Проблема с прямым управлением прогрессом (POST, PATCH, DELETE) от студента
Если студент может отправить:

POST /progress
{ "lessonId": "123", "isCompleted": true }

...или обновить существующий через PATCH, или удалить его через DELETE, — то он сам управляет своим прогрессом, а не система. Это означает:

Он может «пройти» урок, даже не открыв его.
Он может накрутить прогресс через скрипт.
Вы теряете контроль над тем, действительно ли материал усвоен.
Это нарушает логику автоматического отслеживания прогресса. 

✅ Правильный подход: прогресс управляется сервером, а не клиентом
В идеальной системе:

Студент не отправляет POST, PATCH или DELETE для /progress.
Он взаимодействует с контентом: открывает урок, проходит квиз.
Сервер сам решает, когда отметить прогресс как завершённый.
🔧 Как это реализовать?
Вариант 1: Прогресс обновляется после сдачи квиза
Студент проходит урок → читает материал (без API-запроса).
В конце — проходит квиз:

POST /quizzes/{id}/submit
{ "answers": [...] }

Сервер:
Проверяет ответы.
Если тест пройден → автоматически создаёт/обновляет прогресс:
ts

await this.progressService.markLessonAsCompleted(userId, lessonId);

→ Студент не может обмануть систему, потому что прогресс зависит от результата квиза, а не от его заявления.

Вариант 2: Отслеживание через «события» (например, просмотр урока)
Если у вас урок без квиза (только чтение), можно:

Студент открывает урок → фронт отправляет:

POST /lessons/{id}/viewed

Сервер проверяет:
Достаточно ли времени прошло?
Был ли пролистан материал до конца?
Если да → автоматически ставит isCompleted: true.
Но даже здесь: сервер принимает решение, а не доверяет клиенту.





